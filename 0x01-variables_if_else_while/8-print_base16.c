#include <stdio.h>
/**
 * main - Hexadecimal
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char x;
	char z;

	for (x = 48 ; x <= 57 ; x++)
	{
	putchar(x);
	}
	for (z = 97 ; z <= 102 ; z++)
	{
	putchar(z);
	}
	putchar('\n');
	return (0);
}
